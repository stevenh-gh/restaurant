module.exports = {
    purge: [],
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {
            backgroundImage: {
                "food": "url('/resources/food.png')"
            }
        },
    },
    variants: {
        extend: {},
    },
    plugins: [],
}